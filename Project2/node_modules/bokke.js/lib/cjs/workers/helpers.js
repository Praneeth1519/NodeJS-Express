"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createChannel = exports.counter = exports.isDate = exports.isNumber = void 0;
function isNumber(val) {
    if (Number(val)) {
        return true;
    }
    return false;
}
exports.isNumber = isNumber;
function isDate(val) {
    let x = new Date(val);
    if (x instanceof Date && !isNaN(x)) {
        return true;
    }
    else {
        return false;
    }
}
exports.isDate = isDate;
function counter(arr) {
    let count = {};
    arr.forEach((val) => {
        let str = val;
        // if(typeof val !== "string"){
        //    str = val.toString()
        // }
        if (count[str]) {
            count[str]++;
        }
        else {
            count[str] = 1;
        }
    });
    return count;
}
exports.counter = counter;
const createChannel = () => {
    let takes = [];
    let puts = [];
    return {
        put(data) {
            return new Promise(resolvePut => {
                if (takes.length > 0) {
                    takes.shift()(data);
                    resolvePut();
                }
                else {
                    puts.push(() => {
                        resolvePut();
                        return data;
                    });
                }
            });
        },
        take() {
            return new Promise(resolveTake => {
                if (puts.length > 0) {
                    resolveTake(puts.shift()());
                }
                else {
                    takes.push(resolveTake);
                }
            });
        }
    };
};
exports.createChannel = createChannel;
//# sourceMappingURL=helpers.js.map